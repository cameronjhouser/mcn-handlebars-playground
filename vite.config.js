import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages serves from /<repo-name>/ — this must match your repo name exactly.
  // Change this if you rename the repo.
  base: '/mcn-handlebars-playground/',
});
